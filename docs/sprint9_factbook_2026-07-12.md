# BuildBase Sprint 9 / Factbook 実装報告 2026-07-12

## 結果

Sprint 9の状態遷移確認とFactbook主要3社の実値化を実施した。

- Tailscaleの専用共有IPとMac本体IPを区別して表示するようにした。
- 一括却下・照合グループ保存で、事前確認件数と実行件数が変化した場合に警告するようにした。
- previewと実行が同じ対象集合になることをテストで固定した。
- falsey数値候補を確認し、抽出値に関わる代表箇所は上流で数値性を検査済みであることを確認した。即時修正が必要なP0/P1はなかった。
- 鹿島の最新Factbook PDFから用途別受注高を10年度・9分類、計90行抽出した。
- 大林10行、大成100行と合わせ、主要3社の用途別受注高が抽出martへ入った。
- 用途別合計と有報の建築受注高を比較する集計照合を追加した。
- 照合合格行だけを `building_orders_validated.csv` へ出し、グラフは検証済みmartを優先するようにした。
- Factbook画面の内部ステータス語を平易化し、「グラフ利用可」件数を表示した。

## Factbook スコア

| 指標 | 変更前 | 変更後 |
| --- | ---: | ---: |
| 抽出行 | 183 | 273 |
| 鹿島 用途別行 | 0 | 90 |
| 大林 用途別行 | 10 | 10 |
| 大成 用途別行 | 100 | 100 |
| 有報と比較可能 | 0 | 82 |
| グラフ利用可 | 0 | 82 |
| 未確認行 | 178 | 186 |

比較可能82行は鹿島72行・大林10行。鹿島の2024・2025年度は有報側の建築受注高が空欄のため保留した。大成100行は資料が「国内建設事業」、有報側の既存項目が単体建築受注総額で範囲が一致しないため、総額へ無理に対応付けず `no_mapping` のまま保留した。

## 信頼ゲート

`building_orders_by_category.csv` は抽出結果を保持する。資料用グラフは、有報照合に合格した行だけを保存する `building_orders_validated.csv` を読む。不一致、同粒度項目なし、有報値空欄は `company_factbook_pending_rows.csv` に残り、自動採用しない。

## 実画面確認

- ホームの「レビュー候補を見る」から結果画面へ遷移し、状態フィルタが `レビュー候補あり` になることを確認した。
- 絞り込み後、該当セル502件に状態ハイライトが付き、セルクリックでセル作業画面が開くことを確認した。
- Factbook画面で抽出273、比較可能82、グラフ利用可82、照合未完了186が表示されることを確認した。
- グラフAPIが `trust_filter=validated_only` を返し、鹿島・大林の検証済み9会社年度だけを返すことを確認した。

## 検証

- 全436テスト: pass
- `npm run build`: pass
- `audit-state-consistency --fail-on P1`: pass、P0/P1 0
- `regression-check --mode light`: `pass=True`, `mismatch_count=0`
- 実ブラウザ: ホーム、結果フィルタ、セル詳細、Factbookを確認

## 次の優先課題

1. 大成の国内建設事業用途別合計に対応する同範囲の有報値または公式総計行を特定し、独立照合を成立させる。
2. 鹿島2024・2025年度の有報側 `building_orders_total` 空欄を結果表セル起点で補完する。
3. Factbookの完工高パーサを鹿島から追加し、`completed_building_by_use` のカバレッジを増やす。
4. Factbook画面に検証済み／保留の会社・年度内訳と、保留セルへ直接移動する導線を追加する。
